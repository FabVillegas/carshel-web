angular.module( 'carshel-web' ).service( 'uploadModel', uploadModel );

uploadModel.$inject = [];

function uploadModel() {

  function uploadModel() {

  };

  uploadModel.prototype = {

  };

  return uploadModel;

};
