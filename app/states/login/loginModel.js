angular.module( 'carshel-web' ).factory( 'loginModel', loginModel);

loginModel.$inject = [];

function loginModel(){

    function loginModel () {

    };

    loginModel.prototype = {
        exampleMethod: exampleMethod,
    };

    //////////////////////////////////////////////////////////////////////////////

    function exampleMethod() {
      console.log( 'If you are reading this... SHITS FUCKING READY, YO !111!!!one!!!!!1' );
    };

    //////////////////////////////////////////////////////////////////////////////

    return loginModel;

};
